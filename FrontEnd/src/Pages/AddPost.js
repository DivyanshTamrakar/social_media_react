import React from "react";
import styled from "styled-components";

function AddPost() {
  return <PostContainer>This is post tab</PostContainer>;
}

export default AddPost;

const PostContainer = styled.div``;
